[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.builder"

[deploy]
startCommand = "/opt/build-graph.sh"
restartPolicyType = "NEVER"
numReplicas = 1

# Graph builder is a one-off build job
# After successful completion, scale replicas to 0 in Railway dashboard
# Or redeploy to rebuild the graph
